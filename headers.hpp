#ifndef HEADERS
#define HEADERS

#include <iostream>
#include <vector>
#include <fstream>
#include <string>

extern const int ANCHOR;

void newline();

const std::string extract_filename(std::string line);

#endif